{
  "scripts": {
    "build": "./build.sh",
    "watch": "nodemon --watch resume.md --watch 'styles/*.css' --exec './build.sh'",
    "lint": "markdownlint 'resume.md' --ignore node_modules",
    "spellcheck": "cspell 'resume.md'",
    "validate": "node scripts/validate-json.js",
    "convert-json": "node scripts/json-to-md.js",
    "serve": "live-server output"
  },
  "dependencies": {
    "puppeteer": "^24.32.0"
  },
  "devDependencies": {
    "cspell": "^9.4.0",
    "live-server": "^1.2.0",
    "markdownlint-cli": "^0.46.0",
    "nodemon": "^3.1.11"
  }
}
